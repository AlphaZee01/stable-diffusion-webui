# Minimal requirements for img2img virtual try-on functionality
# Core ML and image processing
torch
torchvision
numpy
Pillow
scikit-image>=0.19

# FastAPI for API endpoints
fastapi>=0.90.1
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6

# Essential Stable Diffusion components
accelerate
safetensors
einops
omegaconf
requests

# Image processing utilities
kornia
resize-right
blendmodes

# File handling
GitPython
psutil

# Removed for faster deployment:
# - gradio (not needed for API-only)
# - facexlib (not essential for basic img2img)
# - clean-fid (not needed for try-on)
# - pytorch_lightning (heavy, not essential)
# - torchdiffeq, torchsde (not needed for basic img2img)
# - tomesd (optimization, not essential)
# - open-clip-torch (not needed for img2img)
# - diskcache (not essential)
# - inflection, jsonmerge, lark (not essential)
# - piexif (not essential)
# - protobuf (not essential)
