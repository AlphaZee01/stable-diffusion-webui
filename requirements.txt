# Ultra-minimal requirements for virtual try-on API
# Minimal version for Render starter plan compatibility

# FastAPI for API endpoints
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20

# Basic image processing (no PyTorch)
Pillow==11.3.0
numpy==2.3.2

# HTTP requests
requests==2.32.5

# Removed heavy dependencies for Render starter plan:
# - No torch/torchvision (too heavy for starter plan)
# - No safetensors, einops, omegaconf (SD dependencies)
# - No scikit-image (heavy image processing)
# - No transformers ecosystem
# - No face processing libraries
